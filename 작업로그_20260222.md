# 작업 로그 - 2026년 2월 22일

## 1. API (worker.ts) 수정

### 출발항구 표기 오류 수정
- `/api/rates` 엔드포인트 `port` 필드 누락 문제 확인 및 수정
- `if/else if` → 독립 `if` 문 구조로 변경 (sido, sigungu, eupmyeondong 누적 필터)

### 업체 관리 API 추가
- `GET /api/companies/search` - 업체명 검색 (자동완성)
- `GET /api/companies` - 전체 업체 목록
- `POST /api/admin/companies` - 업체 추가 (중복 체크 포함)
- `PUT /api/admin/companies/:id` - 업체 수정
- `DELETE /api/admin/companies/:id` - 업체 삭제
- 관리자 인증: `adminPassword` 필드 검증
- **port 필드 제거** (업체는 sido/sigungu/eupmyeondong만 저장)

### Fallback 라우트 추가
```typescript
app.get('*', async (c) => {
  const url = new URL(c.req.url)
  url.pathname = '/'
  return c.env.ASSETS.fetch(url.toString())
})
```

---

## 2. DB 스키마 (schema.sql) 수정

### companies 테이블 추가
```sql
CREATE TABLE IF NOT EXISTS companies (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL UNIQUE,
  sido TEXT NOT NULL,
  sigungu TEXT,
  eupmyeondong TEXT,
  created_at TEXT DEFAULT (datetime('now'))
);
CREATE INDEX IF NOT EXISTS idx_company_name ON companies(name);
```

---

## 3. 프론트엔드 (App.tsx) 수정

### 업체 검색 기능 추가
- `Company` 인터페이스 추가
- `companyQuery`, `companySuggestions`, `companyActiveIndex` state 추가
- 업체 검색 `useEffect` 추가 (150ms 디바운스)
- `selectCompany()` 함수: 업체 선택 시 항구/지역 자동 설정 후 운임 조회
- 업체 검색 UI 추가 (행선지 검색 위에 위치)

### 업체 검색 UX 개선
- 엔터키로 첫 번째 항목 선택 기능
- ↑↓ 키보드 탐색
- Escape로 목록 닫기
- 검색어 수정(백스페이스) 시 결과 카드 즉시 초기화

### fetchRates 수정
- `port` 파라미터 선택적으로 받도록 변경: `fetchRates(region, port?)`
- 업체 선택 시 해당 업체의 항구로 운임 조회 가능

### 관리자 버튼 추가
- `<a href="/admin" className="btn-admin">⚙️ 관리자</a>`

---

## 4. 관리자 페이지 (Admin.tsx)

- 업체 추가/수정/삭제 UI
- 비밀번호 로그인 (`admin1234`)
- 업체 목록 테이블
- 메인으로 돌아가기 링크

---

## 5. 라우팅 (main.tsx)

- `/admin` 경로 → `Admin` 컴포넌트
- 그 외 → `App` 컴포넌트

---

## 6. PWA 적용

### 추가 파일
- `public/manifest.json` - 앱 이름, 아이콘, 테마 색상 등
- `public/sw.js` - 서비스 워커 (네트워크 우선, API 제외 캐싱)
- `public/icons/icon-192.png`, `icon-512.png` - 앱 아이콘

### index.html 수정
- `<link rel="manifest">` 추가
- Apple PWA 메타 태그 추가
- 서비스 워커 등록 스크립트 추가

### 캐시 자동 버전 관리
```javascript
const CACHE_NAME = 'safe-rate-' + new Date().toISOString().slice(0, 7)
// 빌드 시점 기준 연-월 자동 생성 (예: safe-rate-2026-02)
```

---

## 7. wrangler.toml 수정

```toml
[assets]
directory = "./dist"
binding = "ASSETS"
```

---

## 8. 배포 정보

- **배포 URL**: https://uwicd.freightrate.workers.dev
- **D1 DB**: freight-rate-db (원격 데이터 배포 완료)

---

## 내일 검토 예정 (프론트엔드)

- UI/UX 추가 개선 사항
- 모바일 레이아웃 점검
- 기타 수정 사항
