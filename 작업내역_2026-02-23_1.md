# 작업내역 요약 - 2026년 2월 23일

## 1. 운임 텍스트 색상 적용
- 정상운임 / 편도운임 → **파란색** (#2563eb)
- 위험물 할증 / 중량 할증 → **빨간색** (#dc2626)
- 원인: `.result-card td { color: #333 !important }` 가 인라인 스타일을 덮어쓰던 문제 수정

## 2. 모바일 관리자 화면 크기 축소
- `h1`, `label`, `input`, `button` 등 전반적인 폰트/패딩 축소
- `.card`, `.admin-form` 패딩 최소화

## 3. 모바일 result-card 개선
- 테이블 구조 → flex 카드 구조로 완전 교체
- 상단: 주소 + 거리
- 하단: `안전운임` 레이블 좌정렬, 40FT / 20FT 운임 가로 나란히 배치
- 테이블 CSS 충돌 문제 근본 해결

## 4. 중량초과 +/- 버튼 추가
- 모바일에서 숫자 스핀버튼 대신 44px 크기의 **－ / ＋ 버튼** 직접 제작
- 기본값 0이 안 지워지는 문제 해결 (input 제거)
- `overweight-row` / `btn-weight` CSS 추가

## 5. 검색 초기화 버튼 추가
- result-options 하단에 `🔄 검색 초기화` 버튼 추가
- 클릭 시 검색어, 업체, 운임결과, 체크박스, 중량 전부 초기화

## 수정된 파일
- `src/App.tsx`
- `src/App.css`
